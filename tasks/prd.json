{
  "project": "tagging-rugby-cli",
  "branchName": "ralph/remove-section-gaps",
  "description": "Remove blank-line gaps between adjacent bordered boxes in Column 3 (Event Distribution / Tackle Stats) and Column 4 (Playback / Navigation / Views).",
  "userStories": [
    {
      "id": "US-001",
      "title": "Remove gap between Event Distribution and Tackle Stats",
      "description": "As a user, I want the Event Distribution and Tackle Stats boxes to stack flush with no blank line between them so vertical space is used efficiently.",
      "acceptanceCriteria": [
        "tui/components/statspanel.go line 169 — change `\"\\n\\n\"` to `\"\\n\"` in the return statement joining eventBox and tackleBox",
        "No blank row appears between the two boxes in Column 3",
        "CGO_ENABLED=0 go build ./... passes",
        "CGO_ENABLED=0 go vet ./... passes"
      ],
      "priority": 1,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-002",
      "title": "Remove gaps between Playback, Navigation, and Views boxes",
      "description": "As a user, I want the Playback, Navigation, and Views control boxes in Column 4 to stack flush with no blank lines between them so vertical space is used efficiently.",
      "acceptanceCriteria": [
        "tui/columns.go renderColumn4 — remove the blank-line gap block: `if i < len(groups)-1 { lines = append(lines, \"\") }`",
        "Remove the comment `// 1 blank line gap between bordered containers` along with the if block",
        "No blank rows appear between Playback, Navigation, and Views boxes in Column 4",
        "CGO_ENABLED=0 go build ./... passes",
        "CGO_ENABLED=0 go vet ./... passes"
      ],
      "priority": 2,
      "passes": true,
      "notes": ""
    }
  ]
}
