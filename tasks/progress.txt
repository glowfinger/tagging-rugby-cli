# Ralph Progress Log
# Branch: ralph/remove-section-gaps
# Started: 2026-03-28

## Codebase Patterns
- When removing a blank-line separator between boxes, also check if the loop variable `i` was only used for that gap logic — change `for i, x := range` to `for _, x := range` to avoid "declared and not used" compile error

## 2026-03-28 - US-001
- Changed `"\n\n"` to `"\n"` in statspanel.go line 169 joining eventBox and tackleBox
- Files changed: tui/components/statspanel.go
- **Learnings for future iterations:**
  - Simple one-line change; build/vet passed immediately
---

## 2026-03-28 - US-002
- Removed the blank-line gap block (`if i < len(groups)-1 { lines = append(lines, "") }`) and its comment from renderColumn4 in tui/columns.go
- Also changed `for i, group := range groups` to `for _, group := range groups` since `i` was only used by the removed gap logic
- Files changed: tui/columns.go
- **Learnings for future iterations:**
  - After removing gap logic that used the loop index `i`, Go compile fails with "declared and not used" — must also remove/replace the index variable
---
