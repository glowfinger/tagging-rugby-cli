# Ralph Progress Log
# Branch: ralph/clip-overlay
# Started: 2026-03-11

## Codebase Patterns
- ffmpeg drawtext escaping: colons in option values need `\\\\:` in Go source (4 backslashes + colon) to survive two levels of filtergraph/option escaping
- The comma in `lt(t,N)` must be escaped as `\\,` to avoid being treated as a filter separator

## 2026-03-11 - US-001
- Updated `clip/processor.go` processClip() drawtext filter from 3-line to 5-line layout
- Added endTimestamp computed from c.End using same `%02d\\\\:%02d\\\\:%02d` format
- Added `player` and `filename` variables with colon escaping via `strings.ReplaceAll`
- All 5 drawtext segments now include `bordercolor=#131211:borderw=2` and use `enable=lt(t\\,2)` (2 seconds, down from 3)
- Line order bottom-to-top: Note ID, Filename, Outcome+Attempt, Player, Time (y offsets 0, 36, 72, 108, 144)
- Files changed: `clip/processor.go`
- **Learnings for future iterations:**
  - PendingClip struct has fields: ClipID, NoteID, Folder, Filename, VideoPath, Category, Player, Attempt, Outcome, Start, End
  - ffmpeg drawtext: `text=Label\\\\: value` renders as "Label: value" after double-escape processing
  - Only `clip/processor.go` was modified — no other files needed changes
---
