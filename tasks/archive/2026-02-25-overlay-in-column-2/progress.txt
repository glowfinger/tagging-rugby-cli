# Ralph Progress Log
# Branch: ralph/overlay-in-column-2
# Started: 2026-02-25

## Codebase Patterns
- SQL embeds: add `//go:embed sql/<name>.sql` to db/queries.go, then use the var in db/functions.go
- RenderInfoBox content lines: pass ` ` (space) + content + ` ` (space) to get visual padding inside borders; innerWidth for content = width - 4
- Tick handler in tui.go: add refresh calls inside `case tickMsg:` before `return m, tickCmd()`
- Column render: always split rendered box on "\n" then append to lines slice; wrap whole column in layout.Container
- ComputeColumnWidths is only called in View() in tui.go — update signature there and in tui/layout/columns.go

---

## 2026-02-25 - US-001
- What was implemented: Added `overlayActive bool` second param to `ComputeColumnWidths` in tui/layout/columns.go; updated call site in tui/tui.go to pass `false`
- Files changed: tui/layout/columns.go, tui/tui.go
- Overlay layout logic: when overlayActive=true: >=170 → Col1+Col2+Col4 (Col2=termWidth-60); 61-169 → Col1+Col2 (Col2=termWidth-30); <=60 → Col1 only
- go vet passes
- **Learnings for future iterations:**
  - ComputeColumnWidths call site in tui.go passes `false` for now — US-002 will replace it with computed `overlayActive`
  - The function signature change is the only thing needed for US-001; no behavior change in the codebase yet
- renderColumn2 early-return pattern: check forms (confirmDiscardForm, noteForm, tackleForm) at the top before normal content; layout.Container wraps form.View() for exact sizing

---

## 2026-02-25 - US-002
- What was implemented: Computed `overlayActive` flag in View() and passed it to ComputeColumnWidths and renderColumn3
- Files changed: tui/tui.go, tui/columns.go
- overlayActive := m.noteForm != nil || m.tackleForm != nil || m.confirmDiscardForm != nil || m.showHelp || m.statsView.Active
- renderColumn3 signature updated to accept overlayActive bool; returns empty Container when true (defensive, since layout won't show col3 when overlayActive anyway)
- go vet passes
- **Learnings for future iterations:**
  - US-003/004/005 will remove the early-return blocks in View() for forms/help/stats; overlayActive is already wired so layout will react correctly once those early returns are gone
  - renderColumn3 is in tui/columns.go (not tui.go) — renderColumn1, renderColumn2 and renderColumn4 are also in columns.go
---

## 2026-02-25 - US-003
- What was implemented: Added form checks at top of renderColumn2 (confirmDiscardForm > noteForm > tackleForm priority); removed 3 full-screen early-return blocks from View()
- Files changed: tui/columns.go, tui/tui.go, tasks/prd.json
- Form check order in renderColumn2: confirmDiscardForm -> noteForm -> tackleForm -> normal search+notes
- Each form returns `layout.Container{Width:width, Height:height}.Render(form.View())` for exact sizing
- truncateViewToWidth() and ControlsDisplay() are now unused by the form overlay paths (orphaned functions but not causing vet errors)
- go vet passes
- **Learnings for future iterations:**
  - US-004 removes the `if m.showHelp` early return; US-005 removes the `if m.statsView.Active` early return — same pattern as US-003
  - After US-003/004/005 the only early returns in View() should be quitting and err
---

## 2026-02-25 - US-008
- What was implemented: Updated TUI-ARCHITECTURE.md to reflect the implemented overlay-in-column-2 design
- Files changed: tui/TUI-ARCHITECTURE.md, tasks/prd.json, tasks/progress.txt
- Removed "Implementation status" note (feature is now implemented)
- Fixed overlayActive computation reference (m.form → m.noteForm || m.tackleForm || m.confirmDiscardForm)
- Updated early returns description (removed "video box" — only quitting and error)
- Updated StatsView/HelpOverlay descriptions (removed "full-screen overlay")
- Updated directory structure comments for help.go and statsview.go
- Updated Esc handler section with full priority list (7 priorities)
- go vet passes
---

## 2026-02-25 - US-007
- What was implemented: Added m.width >= 61 guard to openNoteInput(), openTackleInput(), `?` key handler, `s/S` key handler, and `?` in handleStatsViewInput
- Files changed: tui/tui.go, tasks/prd.json, tasks/progress.txt
- Guard in functions (openNoteInput/openTackleInput): early `if m.width < 61 { return m, nil }` at top
- Guard in global key handler: added `&& m.width >= 61` to existing focus check conditions
- Guard in handleStatsViewInput `case "?"`: wrapped `m.showHelp = true` with `if m.width >= 61 {}`
- Confirm discard is exempt (can only appear if parent form already opened, which passed the guard)
- go vet passes
---

## 2026-02-25 - US-006
- What was implemented: Unified Esc handler at top of tea.KeyMsg case; removed old showHelp any-key-dismissal; removed dead esc handler from handleStatsViewInput
- Files changed: tui/tui.go, tasks/prd.json, tasks/progress.txt
- Unified handler priority: confirmDiscardForm → noteForm → tackleForm → showHelp → statsView.Active → FocusSearch → fall through
- For forms (1-3): delegate to existing handleXxxUpdate(msg) — huh handles Esc internally, preserving discard guard
- For help/stats (4-5): set flag to false; return
- For search (6): m.searchInput.Clear(); m.focus = FocusNotes; return
- Otherwise: fall through to normal handlers (command mode, notes, etc.)
- Removed `case "esc": m.statsView.ClearFilters()` from handleStatsViewInput — was dead code after unified handler intercepts stats Esc
- Behavior change: Esc in stats view now CLOSES stats (not clears filters); Esc in stats filter mode also closes stats
- go vet passes
- **Learnings for future iterations:**
  - "Fall through" for otherwise (don't return) — allows commandInput.Active Esc to still cancel command mode
  - The "any key dismisses help" block (old lines 213-216) was too broad — now only Esc dismisses
---

## 2026-02-25 - US-005
- What was implemented: Added `m.statsView.Active` check in renderColumn2 (after showHelp check); removed `if m.statsView.Active` early return from View()
- Files changed: tui/columns.go, tui/tui.go, tasks/prd.json, tasks/progress.txt
- statsView check: `layout.Container{Width:width, Height:height}.Render(components.StatsView(m.statsView, width, height))`
- StatsView signature: `StatsView(state StatsViewState, width, height int) string` — pass column width/height not terminal dims
- go vet passes
- **Learnings for future iterations:**
  - View() now has only two early returns: quitting and err — all forms/overlays go through column layout
---

## 2026-02-25 - US-004
- What was implemented: Added `m.showHelp` check in renderColumn2 (after form checks); removed `if m.showHelp` early return from View()
- Files changed: tui/columns.go, tui/tui.go, tasks/prd.json, tasks/progress.txt
- showHelp check goes after tackleForm check: `layout.Container{Width:width, Height:height}.Render(components.HelpOverlay(width, height))`
- HelpOverlay(width, height) takes column width+height (not terminal width+height) — overlayActive already computes m.showHelp, so layout expands col2 correctly
- go vet passes
- **Learnings for future iterations:**
  - US-005 removes the `if m.statsView.Active` early return; same pattern — add check in renderColumn2 after showHelp check
  - statsView.Active check: `layout.Container{Width:width, Height:height}.Render(components.StatsView(m.statsView, width, height))`
---

